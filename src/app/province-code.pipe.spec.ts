import { ProvinceCodePipe } from './province-code.pipe';

describe('ProvinceCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ProvinceCodePipe();
    expect(pipe).toBeTruthy();
  });
});
